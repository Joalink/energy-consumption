[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uv run python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healt"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
